error: `main` does not declare the capability `clock`
  --> unauthorised.norn:29:22
   |
29 |         let beeper = spawn reactor Beeper(every: 10)
   |                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ `Beeper` reaches it
   = note: through `Beeper` → `Beeper.on.rang` → `beep` → `sleep`
   = note: a written `uses { … }` is the whole set: write `uses { clock }`, or drop the clause and let it be inferred
