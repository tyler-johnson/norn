error: `Signal` is not a type that can be written
 --> escaping-signal.norn:5:14
  |
5 |     current: Signal<I64>
  |              ^^^^^^^^^^^ a signal lives inside a reactor
  = note: declare it as a member — `signal open = …` — and annotate the element type, as in `signal open: I64 = …`
  = note: outside its reactor it is reached through an `export`, and read with `latest`

error: `Signal` is not a type that can be written
  --> escaping-signal.norn:20:16
   |
20 | fn keep(value: Signal<I64>) -> Event<I64> {
   |                ^^^^^^^^^^^ a signal lives inside a reactor
   = note: declare it as a member — `signal open = …` — and annotate the element type, as in `signal open: I64 = …`
   = note: outside its reactor it is reached through an `export`, and read with `latest`

error: `Event` is not a type that can be written
  --> escaping-signal.norn:20:32
   |
20 | fn keep(value: Signal<I64>) -> Event<I64> {
   |                                ^^^^^^^^^^ an event lives inside a reactor
   = note: declare it as a member — `signal open = …` — and annotate the element type, as in `signal open: I64 = …`
   = note: outside its reactor it is reached through an `export`, and read with `latest`
