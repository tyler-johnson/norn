module hello

struct Greeting {
    salutation: String
    subject: String
}

fn render(greeting: Greeting) -> String {
    return greeting.salutation + ", " + greeting.subject + "!"
}

fn main() {
    let greeting = Greeting(salutation: "Hello", subject: "world")
    print(render(greeting))
}
